fun main
    x = 2
    ax = x
    ax += 63 // should now be 65 ('A')
    param[2] = ax // rcx = ax
    int(80, 4, 1, _, 1)
    int(80, 4, 1, 10, 1) // print a newline (byte 10, 1 of them)
end

// vim: set syntax=c ts=4 sw=4 et:
