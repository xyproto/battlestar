// TODO: Figure out why this program doesn't run.

fun main
    // Output an 'A'
    rax = 65
    int(80, 4, 1, str(rax), 4)

    // Output a newline too
    rax = 10
    int(80, 4, 1, str(rax), 4)
end

// vim: set syntax=c ts=4 sw=4 et:
