const commence = "Commence jump prep.\n"
const nebula = "We're going to the ionian nebula.\n"

fun _start
    int(80, 4, 1, commence, len(commence))
    int(80, 4, 1, nebula, len(nebula))
    int(80, 1)
    ret 2

// vim: set syntax=c ts=4 sw=4 et:
