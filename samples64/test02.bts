extern main

const astring = "A"
const nl = 10

fun printa
    write(astring)
end

fun newline
    write(nl)
end

void main() {
    // Output 'A' 10 times
    for (int i=0; i < 10; i++) printa();
    // And a newline
    newline();
}

// vim: set syntax=c ts=4 sw=4 et:
