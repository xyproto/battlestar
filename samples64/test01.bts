fun main
    rdx = 2    // 2
    rdx *= 2   // 4
    rdx -= 5   // -1
    rdx += 2   // 1
    rdx += 1   // 2
    rdx /= 2   // 1
    rdx *= 3   // 3
    rdx *= 3   // 9
    rdx -= 1   // 8
    rdx += 50  // 58
    rdx += 2   // 60
    rdx /= 6   // 10
    rdx -= 8   // 2
    rdx += 1   // 3
exit rdx // returns with error code 3

// vim: set syntax=c ts=4 sw=4 et:
