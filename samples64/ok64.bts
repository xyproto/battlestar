// TODO: Figure out why this program doesn't output "OK\n"

const o = "O"
const nl = 10

fun main
    // Output a 'O' a newline
    syscall(1, 1, o, len(o))

    // TODO: Figure out why this segfaults
    // Output an 'K'
    rax = 75
    syscall(1, 1, str(rax), 4)

    // Output a newline
    write(nl)
end

// vim: set syntax=c ts=4 sw=4 et:
