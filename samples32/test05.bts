const msg = "Yes", 10

fun main
    // Set edx, fourth parameter on 32-bit
    edx = len(msg)

    // Then use _ to signify that rdx should be used instead of being set when calling the interrupt
    int(80, 4, 1, msg, _)
end

// vim: set syntax=c ts=4 sw=4 et:
