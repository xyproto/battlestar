const o = "O"
const nl = 10

fun main
    // Output an 'O'
    int(80, 4, 1, o, len(o))

    // Output a 'K'
    eax = 75
    int(80, 4, 1, chr(eax), 4)

    // Output a newline
    int(80, 4, 1, nl, len(nl))
end

// vim: syntax=c ts=4 sw=4 et:
