// TODO: Figure out why this program doesn't output "OK\n"

const o = "O"

const nl = 10

fun main
    // Output a 'O' a newline
    int(80, 4, 1, o, len(o))

    // Output an 'K'
    eax = 75
    int(80, 4, 1, chr(eax), 4)

    // Output a newline
    int(80, 4, 1, nl, len(nl))
end

// vim: set syntax=c ts=4 sw=4 et:
